<tr>
  <td><%= question.body %></td>
  <td><%= link_to 'Show answers', admin_question_answers_path(question) %></td>
  <td><%= link_to 'Edit question', edit_admin_test_question_path(@test, question) %> </td>
  <td><%= link_to 'Delete question', admin_test_question_path(@test, question),
    method: :delete,
    data: { confirm: 'Are you sure?' } %> </td>
</tr>
