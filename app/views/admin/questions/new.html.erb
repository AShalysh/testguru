<div class="row justify-content-center mt-3">
  <div class="col col-md-4">
    <h4 class="text-md-center">Create New <%= question_header %> Question. (<%= @test.title %>).</h4>
    <div class="card border-primary">
      <div class="card-body">
        <%= render 'form' %>
      </div>
    </div>
    <p class="mt-3"><%= link_to 'Back', admin_test_path(@test) %></p>
  </div>
</div>
